{
  "type": "object",
  "properties": {
    "end": { "type": "string", "format": "date-time" },
    "id": { "type": "string", "format": "uuid" },
    "is_feedback_visible": { "type": "boolean" },
    "max_team_size": { "type": "integer", "format": "int32", "minimum": 0 },
    "name": { "type": "string" },
    "phase": {
      "type": "string",
      "enum": ["Grading", "Hacking", "Registration", "Finished"],
      "x-readme-ref-name": "EventPhase"
    },
    "slug": { "type": "string" },
    "start": { "type": "string", "format": "date-time" },
    "visibility": {
      "type": "string",
      "enum": ["Private", "Public", "Restricted"],
      "x-readme-ref-name": "EventVisibility"
    }
  },
  "required": [
    "id",
    "name",
    "slug",
    "start",
    "end",
    "max_team_size",
    "is_feedback_visible",
    "visibility",
    "phase"
  ],
  "x-readme-ref-name": "EventDTO"
}
