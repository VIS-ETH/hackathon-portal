# Commands

```bash
npm install
npm run dev # run the development server
npm run build && npm run start # build and run the production server
npm run fmt # format the code, run this before committing
npm run kubb # generate the API client according to src/api/schema.json
```
